@model IEnumerable<Reservation>
@{
    ViewBag.Title = "Quản lý Đặt Bàn";
}

@if (!Model.Any())
{
    <h3 class="text-center">Không tìm thấy đơn đặt bàn nào.</h3>
}
else
{
    <table class="table table-condensed table-bordered">
        <tr class="d-flex center-title">
            <th class="col-md-4">
                Tên khách hàng
            </th>
            <th class="col-md-4">
                Số điện thoại
            </th>
            <th class="col-md-4">Quản Lý</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr class="d-flex center-title">
                <td class="col-md-4">
                    <a href="@Url.Action("Edit", new { id = item.Id })">
                        @item.Name
                    </a>
                </td>
                <td class="col-md-4">
                    @item.PhoneNumber
                </td>
                <td class="col-md-4">
                    <a href="@Url.Action("Edit", "ReservationAdmin", new { id = item.Id })" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                </td>
            </tr>
        }
    </table>
}
